type CircleTimerProps = {
  maxValue: number;
  currentValue: number;
  color: `#${string}`;
  colorsTime: number[];
  size: number;
  fontSize: number;
  strokeWidth: number;
  title: string;
};

export type { CircleTimerProps };
