type CircleTimerProps = {
  maxValue: number;
  currentValue: number;
  color: string;
  colorsTime: number[];
  size: number;
  fontSize: number;
  strokeWidth: number;
  text: string;
  isThousands: boolean;
};

export type { CircleTimerProps };
